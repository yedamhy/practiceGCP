{
  "name": "practicegcp",
  "version": "1.0.0",
  "description": "GCP로 서버 배포 연습",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2" 
  }
}
